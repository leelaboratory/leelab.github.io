source "https://rubygems.org"

# GitHub Pages builds this site with the versions pinned by the github-pages gem.
# To preview locally: bundle install && bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
